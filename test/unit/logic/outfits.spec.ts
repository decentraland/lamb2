import { getOutfits } from '../../../src/logic/outfits'
import { test } from '../../components'

test('AASDF', function ({ components }) {
  it('asf', async () => {
    const address = 'address'
    const outfits = await getOutfits(components, address)

  })
})
