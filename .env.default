# This file should be safe to commit and can act as documentation for all
# the possible configurations of our server.

# This file contains the default environment variables, by default,
# it is third in precedence:
#   1. process environment variables
#   2. `.env` file contents
#   3. `.env.default` file contents.

# HTTP server configuration
HTTP_SERVER_PORT=7272
HTTP_SERVER_HOST=0.0.0.0

# Services URL configuration
CONTENT_URL= #https://peer.decentraland.org/content/
LAMBDAS_URL= #https://peer.decentraland.org/lambdas/
ARCHIPELAGO_URL= #https://peer.decentraland.org/archipelago/
MARKETPLACE_API_URL=https://marketplace-api.decentraland.org/

# Optional: Ethereum network configuration (`mainnet` or `sepolia`, defaults to `mainnet`)
# ETH_NETWORK=

# Optional: Internal URLs for health checks
# INTERNAL_LAMBDAS_URL=
# INTERNAL_CONTENT_URL=
# INTERNAL_ARCHIPELAGO_URL=

# Optional: Name of the realm for the `/about` endpoint
# REALM_NAME=

# Optional: Maximum number of users for the realm
# MAX_USERS=

# Optional: Base URL for NFT worker service (defaults to `https://nfts.decentraland.org`)
# NFT_WORKER_BASE_URL=

# Optional: Custom subgraph URLs
# COLLECTIONS_L1_SUBGRAPH_URL=
# COLLECTIONS_L2_SUBGRAPH_URL=
# ENS_OWNER_PROVIDER_URL=
# THIRD_PARTY_REGISTRY_SUBGRAPH_URL=
# LAND_SUBGRAPH_URL=

# Optional: Service version information
# COMMIT_HASH= Git commit hash for status reporting
# CURRENT_VERSION= Service version for status reporting

NFT_FRAGMENTS_PER_QUERY=10

SUBGRAPH_COMPONENT_QUERY_TIMEOUT=60000
SUBGRAPH_COMPONENT_RETRIES=1

DISABLE_THIRD_PARTY_PROVIDERS_RESOLVER_SERVICE_USAGE=false

DISABLE_CACHE_WARMER_ENABLED=false
